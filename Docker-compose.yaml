services:
  caddy:
    image: caddy
    ports:
      - "80:80"
      - "443:433"
    volumes:
      - ./Caddyfile:/etc/caddy
    networks:
      - caddy_network

  website:
    image: ifc31c-iaw-yeste-toni
    networks:
      - caddy_network

networks:
  caddy_network:
